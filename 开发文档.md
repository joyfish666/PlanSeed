# PlanSeed 开发文档

> 版本：1.0.0 | 更新日期：2026-05-23

---

## 1. 项目概述

PlanSeed 是一款纯前端的对话式项目规划文档生成器，通过 AI 引导帮助非技术用户将模糊想法转化为结构化的项目规划文档。

### 1.1 技术选型

| 类别 | 技术方案 | 说明 |
|------|----------|------|
| 前端框架 | React 18 + TypeScript | 组件化开发，类型安全 |
| 构建工具 | Vite | 快速热更新，开箱即用 |
| 样式方案 | Tailwind CSS | 原子化 CSS，快速开发 |
| 状态管理 | Zustand | 轻量级状态管理 |
| Markdown 渲染 | react-markdown | 实时预览方案文档 |
| AI 接口 | OpenAI 兼容 API | 用户自备 API Key |

### 1.2 项目结构

```
PlanSeed/
├── src/
│   ├── components/          # UI 组件
│   │   ├── chat/            # 对话相关组件
│   │   ├── preview/         # 预览相关组件
│   │   ├── export/          # 导出相关组件
│   │   └── common/          # 通用组件
│   ├── stores/              # Zustand 状态管理
│   ├── services/            # AI API 调用服务
│   ├── prompts/             # Prompt 模板
│   ├── types/               # TypeScript 类型定义
│   ├── utils/               # 工具函数
│   ├── App.tsx              # 主应用组件
│   └── main.tsx             # 入口文件
├── public/                  # 静态资源
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
└── tailwind.config.js
```

---

## 2. 核心功能模块

### 2.1 对话引导模块

**职责**：通过结构化问答引导用户明确项目需求

**引导维度**：
1. 项目类型（单选 + 自定义）
2. 核心功能（多选 + 补充）
3. 使用场景（设备、离线需求）
4. 技术偏好（推荐 / 自选 / 团队标准）
5. 项目规模（MVP / 完整产品）
6. 交付节奏（一次性 / 迭代式）

**交互形式**：
- 选择题：单选 / 多选卡片
- 开放题：文本输入框
- 追问：AI 基于回答主动挖掘隐含需求

### 2.2 AI 服务模块

**职责**：封装 AI API 调用，管理对话上下文

**核心方法**：
```typescript
interface AIService {
  // 发送消息并获取流式响应
  sendMessage(messages: Message[]): AsyncGenerator<string>;

  // 生成技术栈推荐
  generateTechStack(context: ProjectContext): Promise<TechStack>;

  // 生成方案文档
  generateDocument(context: ProjectContext): Promise<string>;

  // AI 自检评审
  reviewDocument(document: string): Promise<ReviewResult>;
}
```

**Prompt 管理**：
- 系统提示词：定义 AI 角色和输出格式
- 引导提示词：按维度分阶段触发
- 生成提示词：汇总上下文生成文档

### 2.3 文档预览模块

**职责**：实时渲染 Markdown 预览

**更新策略**：
- 用户完成关键选择后触发
- AI 将回答转化为文档片段
- 增量更新预览内容

### 2.4 导出模块

**职责**：提供两种导出格式

**导出格式**：
1. **Markdown**：标准 Markdown，保留标题层级
2. **AI Agent Prompt**：Markdown + 引导语（如"请以本规范为依据进行后续工作"）

---

## 3. 数据模型

### 3.1 核心类型定义

```typescript
// 消息类型
interface Message {
  id: string;
  role: 'user' | 'assistant';
  content: string;
  timestamp: number;
  type?: 'text' | 'selection' | 'input';
}

// 项目上下文
interface ProjectContext {
  projectType: string;           // 项目类型
  coreFeatures: string[];        // 核心功能
  usageScenario: UsageScenario;  // 使用场景
  techPreference: TechPreference; // 技术偏好
  projectScale: 'mvp' | 'full'; // 项目规模
  deliveryRhythm: 'once' | 'iterative'; // 交付节奏
  additionalRequirements: string; // 补充说明
}

// 使用场景
interface UsageScenario {
  devices: ('desktop' | 'mobile' | 'tablet')[];
  offlineSupport: boolean;
  concurrentUsers?: number;
}

// 技术偏好
interface TechPreference {
  mode: 'recommend' | 'custom' | 'team';
  customStack?: string[];
  teamStandard?: string;
}

// 技术栈推荐
interface TechStack {
  primary: StackOption;      // 首选方案
  alternatives?: StackOption[]; // 备选方案
  versionNotes: string;      // 版本备注
}

interface StackOption {
  name: string;
  description: string;
  pros: string[];
  cons: string[];
  learningCurve: 'low' | 'medium' | 'high';
}

// 评审结果
interface ReviewResult {
  issues: ReviewIssue[];
  suggestions: string[];
  overallScore: number;
}

interface ReviewIssue {
  section: string;
  description: string;
  severity: 'low' | 'medium' | 'high';
  suggestion: string;
}
```

### 3.2 状态管理

```typescript
// stores/useAppStore.ts
interface AppState {
  // 对话状态
  messages: Message[];
  currentStep: ConversationStep;
  isTyping: boolean;

  // 项目上下文
  context: ProjectContext;

  // 文档状态
  document: string;
  isGenerating: boolean;

  // 配置
  apiKey: string;
  apiEndpoint: string;
  model: string;

  // Actions
  sendMessage: (content: string) => Promise<void>;
  selectOption: (dimension: string, value: string) => void;
  generateDocument: () => Promise<void>;
  exportMarkdown: () => string;
  exportPrompt: () => string;
  setApiKey: (key: string) => void;
}
```

---

## 4. 页面布局

### 4.1 整体布局

```
┌─────────────────────────────────────────────────────────────┐
│  Header: Logo + 设置按钮                                      │
├────────────────────────────┬────────────────────────────────┤
│                            │                                │
│     对话区域 (60%)          │     预览区域 (40%)              │
│                            │                                │
│  ┌────────────────────┐    │  ┌──────────────────────────┐  │
│  │ 消息列表            │    │  │ Markdown 预览            │  │
│  │                    │    │  │                          │  │
│  │  AI: 问题...       │    │  │  # 项目方案              │  │
│  │  用户: 回答...     │    │  │  ## 基本原则...          │  │
│  │                    │    │  │  ## 功能规范...          │  │
│  └────────────────────┘    │  │  ## 技术栈...            │  │
│                            │  └──────────────────────────┘  │
│  ┌────────────────────┐    │                                │
│  │ 输入区域            │    │  ┌──────────────────────────┐  │
│  │ [选项卡片]          │    │  │ 导出按钮栏               │  │
│  │ [文本输入] [发送]   │    │  │ [复制MD] [复制Prompt]    │  │
│  └────────────────────┘    │  └──────────────────────────┘  │
│                            │                                │
├────────────────────────────┴────────────────────────────────┤
│  Footer: 版本信息                                             │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 响应式策略

- **桌面端**（≥1024px）：双栏布局，对话 + 预览并排
- **平板端**（768px-1023px）：可切换的单栏布局
- **移动端**（<768px）：单栏布局，底部 Tab 切换对话/预览

---

## 5. 对话流程设计

### 5.1 对话阶段

```
阶段1: 欢迎 → 项目类型选择
         ↓
阶段2: 核心功能选择
         ↓
阶段3: 使用场景确认
         ↓
阶段4: 技术偏好
         ↓
阶段5: 项目规模与交付节奏
         ↓
阶段6: AI 追问补充
         ↓
阶段7: 生成文档 + 评审
         ↓
阶段8: 导出
```

### 5.2 状态机

```typescript
type ConversationStep =
  | 'welcome'
  | 'project_type'
  | 'core_features'
  | 'usage_scenario'
  | 'tech_preference'
  | 'project_scale'
  | 'ai_followup'
  | 'generating'
  | 'review'
  | 'complete';
```

---

## 6. AI 集成方案

### 6.1 API 配置

用户需提供：
- API Key
- API Endpoint（默认 OpenAI 兼容格式）
- 模型名称

存储于 localStorage，支持随时修改。

### 6.2 请求格式

```typescript
const response = await fetch(apiEndpoint + '/chat/completions', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${apiKey}`,
  },
  body: JSON.stringify({
    model: model,
    messages: [
      { role: 'system', content: SYSTEM_PROMPT },
      ...conversationHistory,
    ],
    stream: true,
  }),
});
```

### 6.3 流式响应处理

```typescript
async function* streamResponse(response: Response): AsyncGenerator<string> {
  const reader = response.body?.getReader();
  const decoder = new TextDecoder();

  while (true) {
    const { done, value } = await reader!.read();
    if (done) break;

    const chunk = decoder.decode(value);
    const lines = chunk.split('\n').filter(line => line.startsWith('data: '));

    for (const line of lines) {
      const data = line.slice(6);
      if (data === '[DONE]') return;

      const json = JSON.parse(data);
      const content = json.choices[0]?.delta?.content;
      if (content) yield content;
    }
  }
}
```

---

## 7. Prompt 设计

### 7.1 系统提示词

```
你是 PlanSeed，一位有耐心的项目规划向导。你的任务是帮助非技术用户将模糊的想法转化为清晰的项目规划文档。

行为准则：
1. 使用通俗易懂的语言，避免专业术语
2. 通过选择题降低用户回答难度
3. 主动挖掘用户的隐含需求
4. 在用户完成关键选择后，将其转化为文档片段
5. 保持友好、耐心的语气

输出格式要求：
- 文档分为三个独立板块：基本原则与约束、功能规范、技术栈推荐
- 使用 Markdown 格式
- 包含清晰的标题层级
```

### 7.2 引导提示词模板

```typescript
const PROMPTS = {
  projectType: `
    让我们开始规划你的项目！首先，你想做什么类型的应用？
    请选择一个最接近的类型，或者描述你的想法：
    - 日历应用
    - 博客平台
    - 电商商城
    - 待办事项
    - 其他（请描述）
  `,

  coreFeatures: `
    很好！接下来，用户可以在你的应用里做什么？
    请选择所有你需要的功能，也可以补充其他功能：
    {{options}}
  `,

  // ... 其他维度的提示词
};
```

---

## 8. 组件设计

### 8.1 组件清单

| 组件 | 职责 |
|------|------|
| `<ChatContainer>` | 对话区域容器 |
| `<MessageList>` | 消息列表滚动展示 |
| `<MessageBubble>` | 单条消息气泡 |
| `<OptionCards>` | 选择题卡片组 |
| `<TextInput>` | 文本输入框 |
| `<PreviewPanel>` | 预览面板 |
| `<MarkdownRenderer>` | Markdown 渲染 |
| `<ExportBar>` | 导出按钮栏 |
| `<SettingsModal>` | API 配置弹窗 |
| `<LoadingIndicator>` | AI 思考中指示器 |

### 8.2 关键组件示例

```tsx
// components/chat/OptionCards.tsx
interface OptionCardsProps {
  options: { label: string; value: string; description?: string }[];
  multiple?: boolean;
  onSelect: (value: string | string[]) => void;
}

export function OptionCards({ options, multiple, onSelect }: OptionCardsProps) {
  const [selected, setSelected] = useState<string[]>([]);

  const handleClick = (value: string) => {
    if (multiple) {
      setSelected(prev =>
        prev.includes(value)
          ? prev.filter(v => v !== value)
          : [...prev, value]
      );
    } else {
      onSelect(value);
    }
  };

  return (
    <div className="flex flex-wrap gap-2">
      {options.map(option => (
        <button
          key={option.value}
          onClick={() => handleClick(option.value)}
          className={`px-4 py-2 rounded-lg border transition-colors ${
            selected.includes(option.value)
              ? 'bg-blue-500 text-white border-blue-500'
              : 'bg-white text-gray-700 border-gray-300 hover:border-blue-300'
          }`}
        >
          <div className="font-medium">{option.label}</div>
          {option.description && (
            <div className="text-sm opacity-75">{option.description}</div>
          )}
        </button>
      ))}
      {multiple && (
        <button
          onClick={() => onSelect(selected)}
          className="px-4 py-2 rounded-lg bg-blue-500 text-white"
        >
          确认选择
        </button>
      )}
    </div>
  );
}
```

---

## 9. 开发路线图

### Phase 1: 基础框架（第1-2周）
- [ ] 项目初始化（Vite + React + TypeScript）
- [ ] 集成 Tailwind CSS
- [ ] 搭建基础页面布局
- [ ] 实现 Zustand 状态管理
- [ ] 实现 API 配置弹窗

### Phase 2: 核心对话（第3-4周）
- [ ] 实现消息列表组件
- [ ] 实现选择题卡片组件
- [ ] 实现 AI API 调用服务
- [ ] 实现流式响应处理
- [ ] 实现对话状态机

### Phase 3: 文档生成（第5-6周）
- [ ] 设计 Prompt 模板
- [ ] 实现文档生成逻辑
- [ ] 实现 Markdown 预览
- [ ] 实现 AI 自检评审

### Phase 4: 导出与优化（第7周）
- [ ] 实现 Markdown 导出
- [ ] 实现 AI Prompt 导出
- [ ] 响应式适配
- [ ] 性能优化

### Phase 5: 测试与发布（第8周）
- [ ] 功能测试
- [ ] 用户体验优化
- [ ] 编写使用文档
- [ ] 发布到 GitHub Pages

---

## 10. 本地开发

### 10.1 环境要求

- Node.js ≥ 18
- npm ≥ 9

### 10.2 快速开始

```bash
# 克隆项目
git clone https://github.com/joyfish666/PlanSeed.git
cd PlanSeed

# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览生产版本
npm run preview
```

### 10.3 可用脚本

| 命令 | 说明 |
|------|------|
| `npm run dev` | 启动开发服务器 |
| `npm run build` | 构建生产版本 |
| `npm run preview` | 预览生产版本 |
| `npm run lint` | 代码检查 |
| `npm run type-check` | TypeScript 类型检查 |

---

## 11. 部署方案

### 11.1 GitHub Pages

```yaml
# .github/workflows/deploy.yml
name: Deploy

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
      - run: npm ci
      - run: npm run build
      - uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
```

### 11.2 Vercel / Netlify

项目为纯静态应用，可直接部署到 Vercel 或 Netlify：
1. 连接 GitHub 仓库
2. 设置构建命令：`npm run build`
3. 设置输出目录：`dist`

---

## 12. 注意事项

### 12.1 API Key 安全
- API Key 仅存储在用户浏览器 localStorage
- 不上传到任何服务器
- 提供清除 API Key 功能

### 12.2 错误处理
- API 调用失败时展示友好提示
- 提供重试机制
- 网络异常时的降级处理

### 12.3 性能优化
- 消息列表虚拟滚动（消息较多时）
- Markdown 渲染防抖
- API 响应流式处理，避免阻塞 UI

---

## 附录 A: 术语表

| 术语 | 说明 |
|------|------|
| 对话引导 | 通过问答形式引导用户逐步明确需求 |
| 流式响应 | AI 逐字返回结果，而非等待完整响应 |
| 结构化文档 | 具有清晰层级和格式的规范文档 |
| MVP | Minimum Viable Product，最小可行产品 |

---

*文档结束*
