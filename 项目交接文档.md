# PlanSeed 项目交接文档

> 生成日期：2026-08-05 ｜ 版本基线：v1.1.0
> 面向对象：后续接手本项目的开发者 / AI 助手

---

## 一、项目现状

### 1. 双形态架构

PlanSeed 以两种形态交付同一套规划逻辑：

| 形态 | 技术栈 | 位置 | 是否需要 API Key |
|------|--------|------|------------------|
| **Web App** | React 19 + TS + Vite 8 + Zustand 5 + react-markdown | 仓库根 `src/` | 需要（浏览器 localStorage 存储） |
| **Agent Skill** | Node + `@modelcontextprotocol/sdk` + zod | `skills/`（独立 npm 包） | 不需要（复用宿主 Agent 模型） |

### 2. 两个"设计灵魂"（重构时务必理解）

- **Web App：AI 全权驱动对话。** `SYSTEM_PROMPT`（`src/prompts/index.ts`）定义了全部流程（单问题原则、必填信息清单、功能确认循环、技术栈推荐、审查），前端只做三件事：渲染消息、解析 `[OPTIONS:...]` / `[Q&A]` / `[REVIEW_PASS:...]` 标记、展示/导出文档。**store 里没有对话状态机**——项目上下文 `context`、`currentStep`、`reviewStage` 等已在 v1.1.0 删除。
- **Agent Skill：纯状态机流水线。** `skills/src/pipeline.ts` 依序执行 7 个 skill，每个 `execute()` 返回结构化 prompt 给宿主 Agent 解读。服务器**不调用任何外部 AI API**，因此没有 API Key、没有模型配置，文档最终由宿主 LLM 生成。

### 3. 质量基线（v1.1.0 起）

- `npm run type-check` ✓（且已开启 `noUnusedLocals`/`noUnusedParameters`）
- `npm run lint` ✓（v1.0.0 时是失败的，已修复并纳入 CI）
- `npm run build` ✓
- `cd skills && npm run build` ✓

### 4. 版本号位置（改版时需同步四处）

- 根 `package.json`：`"version": "1.1.0"`
- `skills/package.json`：`"version": "1.1.0"`
- `skills/src/index.ts`：`McpServer({ version: "1.1.0" })`
- `src/App.tsx` 页脚：`PlanSeed v1.1.0`

---

## 二、v1.1.0 变更清单

### 修复的 Bug

| 模块 | 问题 | 修复 |
|------|------|------|
| Skill | `get_document` 永远取不到真实文档：MCP 服务器看不到宿主 LLM 产出，且 `includes("#")` 误把指令模板当文档 | 新增 `save_document` 工具，由宿主生成/修改文档后回传；移除损坏的捕获逻辑 |
| Skill | tech-stack 依赖文本关键词（React/Vue/推荐）判断选择，不命中即死循环 | 重写为显式状态机（`techStack.stage` 0→4），任意输入均记录，并显式采集项目规模与交付节奏 |
| Skill | `"不需要离线支持".includes("需要")` 为真，离线支持语义反转 | 判定改为 `includes("需要") && !includes("不需要")` |
| Skill | 功能确认循环中新增的功能只写进 `conversationHistory`，最终文档丢失 | 新增功能累积进 `coreFeatures`，并引入 `featureStage` 区分"收集"与"修改"阶段 |
| Web | 每轮对话触发 2 次 API 调用（流式回复 + 全量预览） | v1.1.0 曾改为"仅首次自动预览"以省成本；v1.1.1 根据产品需求恢复每轮自动预览（实时反馈优先），手动"刷新预览"保留为兜底 |
| Web | `SettingsModal` 渲染期读取 ref（lint error，且提示不随状态刷新） | `keyModifiedRef` 改为 state `keyModified` |
| Web | API Key 误清：聚焦掩码输入框清空后直接保存会存成空串 | 保存时仅当"已修改且非空"才覆盖 |
| Web | 欢迎语与"请先设置参数"文案矛盾，且 StrictMode 下会重复 | 欢迎语仅在有 Key 且对话为空时注入，文案去矛盾 |
| Web | lint 7 error（含 `catch (error)` 未使用变量） | 全部修复，lint 转绿 |

### 清理的死代码

- **Web**：`OptionCards.tsx`、`LoadingIndicator.tsx`、`formatDate`、store 中的 `selectOption`/`updateContext`/`context`/`reviewStage`/`generateDocument`、类型 `ProjectContext`/`UsageScenario`/`TechPreference`/`TechStack`/`ReviewResult`/`ReviewIssue`/`ConversationStep`/`Message.type`、未使用的分步 prompt 常量。
- **Skill**：整个 `prompts/` 目录（保留 `base-persona.ts`）、`message-parser.ts`、`runPipelineHeadless`、`types.ts` 中重复声明的 `PipelineRunner`。

### 行为变化（文档已同步）

- 预览面板：每次 AI 响应后自动更新（让用户实时看到文档变化）；手动"刷新预览"作为兜底。
- Agent Skill：文档持久化依赖宿主调用 `save_document`；"保存文档到当前目录"选项保留。

### v1.1.1 修订（2026-08-05）

- 恢复 Web 端**每轮自动预览**：用户每次做出选择/修改后，文档实时反映变化（v1.1.0 为省成本改为"仅首次自动生成"，收到产品反馈后撤销）。API 成本作为已接受的产品决策，手动"刷新预览"保留为兜底。
- 删除仓库根目录的 `SYSTEM_PROMPT.txt`（与 `src/prompts/index.ts` 重复的冗余副本，消除双源漂移）。
- 版本同步至 1.1.1（四处）。

---

## 三、过程中遇到的坑（务必阅读）

1. **不要在 stdio MCP 服务器里"捕获"宿主产出。** MCP 服务器只能看到工具入参/返回值，看不到宿主聊天内容。`get_document` 想要真实文档，只能靠宿主主动回传（`save_document`）。任何基于宿主回复的猜测性捕获（如 `includes("#")`）都是不可靠的。
2. **状态机不要用文本关键词判断用户意图。** `userInput.includes("React")` 这类判定会把"Flutter""微信小程序"等一切不命中的输入丢进死循环。需要多步采集时，务必在 context 里放显式 `stage`/`phase` 字段。
3. **中文否定词子串陷阱。** `"不需要离线支持".includes("需要") === true`。判定语义状态时先排除否定形式。
4. **状态机必须把新增数据写回 context。** Skill 的 `execute` 返回的 `context` 是下一个 skill 的唯一输入源。只往 `conversationHistory` 里追加、不更新业务字段，后续 skill 就拿不到该数据。
5. **React 19 + eslint 会拦截渲染期读 ref**（`react-hooks/refs`），且 ref 变化不触发重渲染。需要"随渲染更新的标记"一律用 `useState`。
6. **掩码输入框的聚焦清空副作用。** "聚焦时清空 `***` 供编辑"会在用户只是点了一下输入框时就把字段清空；若随后保存，会覆盖已有 Key。必须加"非空才覆盖"守卫。
7. **每轮对话的"全量文档预览"是隐形成本。** 把预览生成放逐到每条消息后，会带来约 2 倍的 API 调用与随对话增长的 prompt。让预览"按需生成"。
8. **React StrictMode 下 effect 会双跑。** 没有守卫的"首次注入欢迎语"会出现重复消息，加 `messages.length === 0` 条件。
9. **`SYSTEM_PROMPT.txt`（仓库根，未跟踪）** 是 `src/prompts/index.ts` 中 `SYSTEM_PROMPT` 的冗余副本（内容一致，仅换行符不同）。它是开发遗留，建议删除或移入版本管理，避免双源漂移。
10. **`skills/.mcp.json` 含本机绝对路径**（`C:/Users/wang/...`），已提交但会让其他用户克隆后失效。注册 MCP 请用 README 中的 `claude mcp add` 方式，或改用相对 `skills/mcp.json`（依赖 cwd）。
11. **根 CI 不构建 skills 包。** `skills/` 有独立 `package-lock.json` 与依赖，根目录的 `npm ci` 不会安装它们。skills 的回归目前只能靠本地 `cd skills && npm run build`。
12. **解析含 Markdown 代码围栏的模板字符串时，正则要小心。** `SYSTEM_PROMPT` 内含 ` ``` ` 围栏，用 `/\`([\s\S]*?)\`;/` 之类 naive 正则会在第一个反引号处截断。

---

## 四、下一步建议

### 短期（正确性 / 质量）

1. **为 skills 流水线补单元测试**：`feature-confirmation` 累积、`tech-stack` 状态机各阶段、`usage-scenario` 离线否定判定、`pipeline` 推进。目前零测试，这些是纯函数、非常可测。
2. **CI 增加 skills job**：`cd skills && npm ci && npm run build && npm test`，防止 skill 代码回归（它当前完全游离于 CI 之外）。
3. **Web 请求可取消**：给 `aiService.sendMessage` 接入 `AbortController`，UI 提供"停止生成"按钮（流式长回复时体验更好）。
4. **处理遗留**：删除 `SYSTEM_PROMPT.txt`；规范化 `skills/.mcp.json`。

### 中期（功能）

5. **Web 显式"最终文档"步骤**：当前最终文档 = 最后一次预览。可把被删的 `generateDocument` 重新接成一个显式按钮/流程，产出更干净的终稿（预览模式带"（待补充）"占位，终稿模式更完整）。
6. **Prompt 单一来源**：Web 的 `SYSTEM_PROMPT` 与 Skill 的 `base-persona` + 各 skill prompt 已分叉。若后续两个形态要严格一致，可抽象一份共享的流程定义（但需权衡：Web 是单 prompt 让 AI 自由发挥，Skill 是确定性状态机，本质不同，强行统一可能两头受损）。
7. **导出增强**：PDF / JSON 结构化导出、文档模板选择。

### 长期（产品）

8. **多语言**：目前 prompt 与 UI 均为中文，可抽象 i18n。
9. **离线能力**：Web App 目前必须联网调用 AI，可考虑本地小模型 fallback。
10. **发布渠道**：npm 发布 `planseed-mcp` 包、Web 端接入 Vercel Preview 等。

---

## 五、关键文件索引

| 关注点 | 文件 |
|--------|------|
| Web 对话/状态 | `src/stores/useAppStore.ts` |
| Web AI 调用/文档生成 | `src/services/ai.ts` |
| Web 系统提示词 | `src/prompts/index.ts` |
| Web 消息标记解析 | `src/utils/index.ts` |
| Web UI 组件 | `src/components/chat/*`、`preview/*`、`export/*`、`common/*` |
| Skill MCP 入口与工具 | `skills/src/index.ts` |
| Skill 流水线 | `skills/src/pipeline.ts`、`skills/src/types.ts` |
| Skill 各阶段逻辑 | `skills/src/skills/*.ts` |
| Skill 共享人格 | `skills/src/prompts/base-persona.ts` |
| 工程配置 | `tsconfig*.json`、`vite.config.ts`、`eslint.config.js`、`.github/workflows/deploy.yml` |
| 文档 | `README.md`、`README-zh.md`、`开发文档.md`、`skills/README.md` |

---

*文档结束*
